# dbos-kickstart

Post-install setup for CachyOS + Hyprland: GNOME Keyring, Git Credential Manager, and Git credentials via secret service.

```bash
curl -fsSL https://raw.githubusercontent.com/DigitalBastion/dbos-kickstart/main/kickstart.sh | bash
```
